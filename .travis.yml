sudo: required

services:
  - docker

env:
  global:
  - DOCKER_REPO=nhsuk/profiles-db
  - DOCKER_USERNAME=nhsukautomata

after_success:
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - ./ci-deployment/docker_ci.sh

